# 三角色虚拟账号需求测试执行交接

更新时间：2026-08-12  
项目：社区助手  
CloudBase 环境：`cloud1-d2g3tkgc73fb08aa8`

## 1. 测试账号与夹具

已创建带 `TEST_` 前缀的隔离数据库测试夹具：

| 身份 | 虚拟 OpenID | 用户记录 ID | 角色状态 |
|---|---|---|---|
| 住户 | `TEST_RESIDENT_20260812_OPENID` | `TEST_USER_RESIDENT_20260812` | 已认证业主 `owner` |
| 管理员 | `TEST_ADMIN_20260812_OPENID` | `TEST_USER_ADMIN_20260812` | 系统管理员 `system_admin` |
| 商家 | `TEST_MERCHANT_20260812_OPENID` | `TEST_USER_MERCHANT_20260812` | 已认证商家 `merchant` |

关联测试数据：

- 邀请码：`TEST-20260812`，楼栋/房号：`10-1902`
- 住户已发布帖子：`TEST_POST_RESIDENT_20260812`
- 商家待审核帖子：`TEST_POST_MERCHANT_20260812`
- 已审核商家资料：`TEST_MERCHANT_APPLICATION_20260812`
- 已授权装修需求：`TEST_RENOVATION_REQUEST_20260812`
- 商家线索分配：`TEST_LEAD_ASSIGNMENT_20260812`

夹具脚本：[`scripts/role-test-fixtures.mjs`](../scripts/role-test-fixtures.mjs)

注意：这些是数据库层虚拟身份，不是可以直接在微信中登录的账号。CloudBase 的 `OPENID` 来自真实微信会话，`tcb fn invoke` 没有提供模拟调用者 OPENID 的参数，因此不能仅凭这些字符串完成真实小程序登录。

## 2. 已执行结果

| 编号 | 场景 | 结果 | 证据/备注 |
|---|---|---|---|
| F-01 | 创建三种身份及关联业务数据 | 通过 | 夹具脚本 9 组记录均写入成功，使用固定 ID 可重复执行 |
| F-02 | 公开商家目录读取 | 通过 | `merchantDirectory` 返回 `TEST_装修服务商_20260812`；返回内容不含商家联系方式 |
| F-03 | 交付中心公开读取 | 通过 | `deliveryHub` 返回交付日期、社区名、更新列表和资料列表结构 |
| F-04 | 公开广告列表匿名调用 | 通过 | 2026-08-12 修复并部署后复测，返回 `ads: []`，`InvokeResult: 0` |
| F-05 | 公开议题列表匿名调用 | 通过 | 2026-08-12 修复并部署后复测，返回 `topics: []`，`InvokeResult: 0` |
| F-06 | 住户帖子公开列表 | 未完成 | CloudBase CLI 二次调用的自动权限审批超时，不能归因于产品功能失败 |
| F-07 | 三角色登录后端到端操作 | 未完成 | 需要真实微信测试会话，或本地测试替身注入 OPENID；不建议给生产函数增加测试后门 |

## 3. 角色需求覆盖状态

住户夹具已具备：已认证业主、已发布帖子、已授权装修需求，可继续验证发帖、评论、点赞/收藏、举报、装修需求撤回授权。

管理员夹具已具备：系统管理员身份、待审核商家帖子、已授权装修需求和商家，可继续验证内容审核、线索分配、广告位维护、隐私申请与运营统计。

商家夹具已具备：已认证商家、装修/广告/团购板块权限、待审核帖子、已审核商家资料、已分配装修线索，可继续验证发帖审核、线索读取、联系状态流转和目录展示。

以上“可继续验证”不等于已经通过；由于当前调用链没有真实 OPENID，本轮不能把带权限的操作标记为最终通过。

## 4. 修复结果与建议

1. `adManage` 的公开 `list` 已在鉴权前读取公开广告，`communityTopics` 的 `userOf` 已支持空 OpenID；两项匿名调用线上复测通过。
2. 真正验收三种身份时，应使用三个真实微信测试会话，并分别执行：住户发帖/互动/装修授权，管理员审核/分配/运营，商家发帖/线索状态流转/目录展示。
3. 测试数据当前保留，供下一会话继续使用；不得将 `TEST_` 联系方式当作真实联系方式展示或外呼。

## 5. 清理命令

确认不再需要夹具后，在项目目录执行：

```powershell
& 'D:\Program Files\nodejs\node.exe' scripts\role-test-fixtures.mjs cleanup
```

该命令只删除本交接文件列出的 `TEST_` 前缀测试记录。
